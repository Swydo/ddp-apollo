sudo: required
language: node_js
node_js:
  - "8"
addons:
  chrome: stable
before_install:
  - "curl https://install.meteor.com | /bin/sh"
  - export PATH="$HOME/.meteor:$PATH"
  - meteor --version
script:
  - "npm run eslint -s"
  - "npm test -s"
